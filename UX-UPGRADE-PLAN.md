# OpenClaw 桌面宠物 — 交互体验升级方案

> 制定日期：2026-03-27

## 当前核心问题

| 问题 | 描述 |
|------|------|
| **等待状态残废** | 只有静态 `···` 气泡，几秒就消失，复杂指令处理 5-30 秒用户完全不知道状态 |
| **没有流式响应** | `chatStream` 名不副实，实际是等 CLI 完全执行完才一次性返回 |
| **长文本无法查看** | 展开气泡用 PixiJS 画的，没有滚动条，超出 mask 的内容永远看不到 |
| **猫咪像死的** | 无交互时完全静止，没有闲置动画，没有生命感 |
| **无主动交互** | 不会打招呼、不会提醒、不会主动冒泡 |
| **输入框体验差** | 鼠标进出闪烁，没有全局快捷键，没有可视化语音按钮 |
| **错误处理粗糙** | 连接失败只有一句话，没有重试、没有离线检测 |

## 升级方案（四阶段）

### Phase 1：对话体验（P0，最紧急）

#### 1. 真正的流式响应
- main.js 的 `child.stdout.on('data')` 改为实时推送 `chat-chunk` 到渲染进程
- 气泡逐字追加显示，配合打字机动画
- 猫咪在接收流式输出时播放"说话"微动画

#### 2. 完整的等待状态
- 三个跳动圆点动画替代静态 `···`
- 阶段性文字：3s "想想..." → 10s "认真想..." → 20s "这个比较难喵..." → 45s 显示取消按钮
- 等待期间猫咪切换"思考"姿态（头部微倾，用 Live2D 参数控制）
- **等待气泡不会自动消失**，直到收到响应或超时

#### 3. 长文本改用 DOM 覆盖层
- 气泡显示摘要（最多 3 行），点击/hover 弹出 HTML DOM 浮层
- DOM 浮层支持原生滚动、文本选择、复制
- 苹果风圆角毛玻璃样式（CSS backdrop-filter: blur）
- 保留像素风气泡作为摘要入口

### Phase 2：生命感（P1）

#### 4. 闲置行为状态机
- 每 20-50 秒随机触发一个闲置动作
- 动作库：微呼吸（scale 缓慢波动）、左右张望、打哈欠、跳跃、摇晃
- 加权随机（刚做完的动作短时间不会重复）
- 时段感知：22:00 后增加打哈欠频率，早上多伸懒腰

#### 5. 主动问候系统
- 启动时按时段问候："早上好主人！" / "深夜了还不睡喵？"
- 长时间无交互（>30min）主动冒泡："记得喝水喵~"
- 每日首次打开的特殊问候

### Phase 3：实用功能（P2）

#### 6. 番茄钟 / 提醒系统
- 右键菜单增加"提醒"子菜单（25min 番茄钟、自定义倒计时、喝水间隔）
- 到时间猫咪跳起 + 气泡 + 猫叫

#### 7. 全局快捷键呼出
- `Cmd+Shift+P` 直接聚焦输入框，不需要先移动鼠标到猫咪上
- 参考 ChatGPT Desktop / Claude Desktop 的核心交互

#### 8. 错误处理与重试
- 区分：网络错误 / OpenClaw 未启动 / 超时
- 气泡内提供重试按钮
- 检测 Gateway 状态，离线时猫咪旁显示灰色圆点

### Phase 4：深度养成（P3）

#### 9. 对话历史面板
- 右键菜单打开，DOM 面板显示最近 20 条对话

#### 10. 情绪系统
- 根据交互频率维护心情值，影响闲置动画选择

#### 11. 输入框优化
- textarea 多行、语音按钮可视化、Shift+Enter 换行

#### 12. 拖拽吸附
- 屏幕边缘自动吸附，记住位置

## 调研参考

- BlackBearCC/openclaw-pet — Canvas 气泡 + DOM 展开面板混合渲染
- ChatGPT/Claude Desktop — 流式逐字显示 + 全局快捷键
- desktoppet.app — 番茄钟/喝水提醒集成
- VPet-Simulator — 384 种动画状态
- Shimeji — 闲置行为状态机 + 加权随机
- Cloudscape/patterns.dev — AI 加载状态最佳实践

## 关键文件

- 主进程：`main.js`
- 渲染页面：`index.html`
- 预加载桥接：`preload.js`
- 喵喵人格：`~/openclaw-workspace/agents/companion/neko-assistant.md`
